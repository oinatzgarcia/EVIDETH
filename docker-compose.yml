# docker-compose.yml eliminado — el simulador Docker ha sido reemplazado
# por el cliente web (viewer.html en puerto 8080).
#
# Para desarrollo local usa docker-compose.dev.yml:
#   docker compose -f docker-compose.dev.yml up -d
